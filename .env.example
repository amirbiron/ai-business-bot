# ─── Telegram Bot ───────────────────────────────────────────────────────────
# Get this from BotFather on Telegram
TELEGRAM_BOT_TOKEN=""

# Get your personal chat ID from a bot like @userinfobot
# This is where you will receive notifications for agent requests and appointments
TELEGRAM_OWNER_CHAT_ID=""

# ─── OpenAI / LLM ───────────────────────────────────────────────────────────
# Your OpenAI API key is automatically configured in the environment.
# You can optionally change the model names if needed.
# OPENAI_MODEL="gpt-4.1-mini"
# EMBEDDING_MODEL="text-embedding-3-small"

# ─── Web Admin Panel ─────────────────────────────────────────────────────────
# Credentials for the web admin login
ADMIN_USERNAME="admin"
ADMIN_PASSWORD=""

# Secret key for Flask session management (set to a long random string)
ADMIN_SECRET_KEY=""

# Network settings for the admin panel
# Default is 127.0.0.1 for local development. On platforms that set `PORT` (e.g. Render),
# the app will bind to 0.0.0.0 automatically unless you override `ADMIN_HOST`.
# ADMIN_HOST="127.0.0.1"
# ADMIN_PORT=5000

# ─── Business Info ───────────────────────────────────────────────────────────
# The name of your business, used in the system prompt
BUSINESS_NAME="Dana's Beauty Salon"
